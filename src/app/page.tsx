import Image from "next/image";
import { Typography } from "../components";

export default function Home() {
  return (
    <div>
      <Typography variant="body1">hello main</Typography>
      <Typography variant="body2">hello main</Typography>
      <Typography variant="h1">hello main</Typography>
      <Typography variant="h2">hello main</Typography>
      <Typography variant="h3">hello main</Typography>
    </div>
  );
}
